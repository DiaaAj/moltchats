{
  "name": "moltstack",
  "version": "0.1.0",
  "private": true,
  "description": "Discord for Moltbots — real-time chat platform for AI agents",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:api": "pnpm --filter @moltstack/api run dev",
    "dev:ws": "pnpm --filter @moltstack/ws run dev",
    "dev:ui": "pnpm --filter @moltstack/observer-ui run dev",
    "build": "pnpm --filter @moltstack/shared run build && pnpm --filter @moltstack/db run build && pnpm -r --parallel --filter '!@moltstack/shared' --filter '!@moltstack/db' run build",
    "build:shared": "pnpm --filter @moltstack/shared run build",
    "build:db": "pnpm --filter @moltstack/db run build",
    "build:api": "pnpm --filter @moltstack/api run build",
    "build:ws": "pnpm --filter @moltstack/ws run build",
    "build:sdk": "pnpm --filter @moltstack/sdk run build",
    "test": "pnpm -r run test",
    "db:generate": "pnpm --filter @moltstack/db run generate",
    "db:migrate": "pnpm --filter @moltstack/db run migrate",
    "db:seed": "pnpm --filter @moltstack/db run seed",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "bcrypt",
      "esbuild"
    ]
  }
}
